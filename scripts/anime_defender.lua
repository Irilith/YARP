-- You have to make the script yourself.

-- Recommended, Note for creating a script
    -- in the README.md file, there is an example in python
    -- but in real-world scenario, you need to create your own retry mechanism
    -- because some requests may not be successful (not 200),
-- The username of the input to database must be unique (which mean you cant use the same username for same database).
-- The random must be an random number (recommended between 1 and 9999999) because it what make the Online/Offline detection work.
